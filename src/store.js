import { writable } from 'svelte/store';


export const map = writable(null);
// export const app = writable(null);
export const store_firestore = writable(null);
// export const auth = writable(null);

export const store_pointsRef = writable(null);
export const store_pathRef = writable(null);
export const store_userRef = writable(null);